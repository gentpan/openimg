# CHANGELOG

openimg.io 的变更记录，只写用户与自建者能感知到的部分。本项目不发版本号、直接部署，所以按部署日期分段，新的在上面。标注「自建者需动手」的段落是升级自建实例时需要配合改动的内容，只改代码不做这些事会得到半截配置。

---

## 2026-08-20

### 新增
- AI 生成记录可以在列表里删除；有产出图时会让你选「只删记录，图片留在图库」还是「记录和图片一起删」。正在生成中的记录不给删（额度已扣、上游可能还在出图）。删除不退回当天已用掉的生成次数。
- 新增 `DELETE /api/tokens/current`：客户端退出登录时可以让当前这枚 API 令牌自己作废。此前退出只是把令牌从本机抹掉，服务器上那枚照旧有效，得自己去网站的账号设置里删。

### 变更
- 签到的「一天」改按账号所在时区计算，不再一律按 UTC——此前东八区用户的日界落在本地早上 8 点。网页端登录后会自动上报一次浏览器时区；没上报过的老账号仍按 UTC，行为与以前完全一致。签完后把时区往西调也不能再签一次。
- 主题绿由 `#5DE31D` 换成 `#90FF3A`，主题紫由 `#8E47FF` 换成 `#7624F4`，整条色阶重算。网站图标、PWA 安装图标与启动色、社交分享图、邮件模板一并跟着换。
- 顶部导航换成滑动发光标签：一根细条停在当前页下方，换页时滑过去。当前页不在导航里时（如设置页）细条留在原地；系统开了「减少动态效果」时不做滑动。

### 修复
- 在浏览器里直接打开 CDN 上的图片，恢复居中显示，不再贴在左上角。此前 `cdn` 域名上的 CSP 连带禁掉了浏览器内置图片查看器用来居中的内联样式。
- 社交分享图的 URL 带上内容哈希，换图之后已经转发出去的链接不会再一直展开旧卡片（微信没有重新抓取的入口，换 URL 是唯一的作废手段）。PWA manifest 里的图标故意不加指纹。
- 短链域名上访问不是短码的路径（如 `/login`、`/settings`），现在 301 跳回主站，不再在第二个域名上开出一个完整站点——那边的 cookie 域对不上，也会被搜索引擎当成重复内容。

> **自建者需动手**
> - `deploy/caddy/openimg.caddy` 去掉了 cdn 域名上的 `Content-Security-Policy: default-src 'none'`。自己维护 Caddy 配置的需要同步这一处，否则图片仍不居中。服务端本来就显式拒绝 SVG，防护依赖 `nosniff` + 正确的 Content-Type + 独立域名。
> - 数据库新增两列（`users.timezone`、`ai_generations.hidden`），启动时 AutoMigrate 自动添加，无需手动迁移。`timezone` 留空即按 UTC，升级不会挪动任何老账号的日界。
> - 短链域名跳回主站依赖 `ShortBaseURL` / `PublicBaseURL` 两项配置都填对，否则不会跳转（退回原来的 SPA 未找到页）。
> - 若改过品牌色：`frontend/public/` 下的十几个 PNG/ICO 是 `frontend/scripts/gen-favicons.sh` 从 `frontend/brand/*.svg` 渲染出来的，改完 SVG 必须重跑脚本，否则图标不会跟着变。

---

## 2026-08-19

### 新增
- AI 生成接口新增 `purpose=watermark` 用途，供客户端做 AI 水印：固定 1:1、1k 档（水印按画面宽度的十几个百分点渲染，更高档位的像素用不上，而 4K 恰恰最贵），输出纯色底的图，透明背景由客户端本机抠；抠不出来也不算失败，那张带底的水印仍然可用。

### 变更
- 图片直连域名从 `cdn.imgla.com` / `cache.imgla.com` 切到 `cdn.openimg.io` / `cache.openimg.io`，imgla.com 停用。页面上取到的地址是读时现拼的，会自动变成新域名；但此前复制到别处的旧域名外链需要自行留意。
- 短链改挂独立域名 `163.uz`，图库列表、分享页、短链自身给出的都是同一个新地址（原主站形式的短链仍可打开）。该域名只放短码、分享页资源和分享页接口，其余路径 301 回主站。
- AI 两页的额度显示改版：数字从独立卡片挪进输入区底排，只在用完时才单独显示一条提示，并区分「今天用完了」（等明天）与「本月用完了」（去签到）两种情况。
- 修图预设从 5 个增加到 11 个，新增：去文字、白底商品图、黑白上色、人像精修、卡通风格、扩展画面。未选中的预设加了描边，不再看着像纯文字。

### 修复
- 在浏览器里直接打开一张图，图片恢复居中显示——CDN 响应去掉了 CSP 的 `sandbox`，它把响应放进沙箱上下文，浏览器内置图片查看器因此把图贴在左上角。

> **自建者需动手**
> - 新增可选变量 `SHORT_BASE_URL`：短链挂的域名，留空即与 `PUBLIC_BASE_URL` 同域，行为与加这个配置之前完全一致——自建部署多半只有一个域名，不必再买一个。
> - 更换 CDN 域名要改三处，少一处就是半截配置：Caddy 站点块的域名与证书、`.env` 的 `S3_PUBLIC_URL_BASE` / `S3_THUMB_URL_BASE`、以及**数据库里那行平台存储配置**——`EnsurePlatformProfile` 只在记录不存在时插入，已有的行不会被 `.env` 的改动带上。
> - `deploy/deploy.sh` 的验收多了一条：向 CDN 域名取一个不存在的对象必须拿到 404（502/503 说明代理断了，200 说明桶列表泄露了）。只看根路径 301 证明不了 MinIO 那条 rewrite 还对。

---

## 2026-08-18

### 新增
- 新增「AI 生成」页：写一句描述就出图，成图直接进图库，与手动上传的图完全一样（去重、外链、短链、备份都照旧），可选 7 种画面比例。
- 新增「AI 修图」页：从图库挑 1~4 张图（或直接把本地文件拖进来上传），写一句要改什么；带「去水印 / 去除路人杂物 / 换背景 / 修复老照片 / 提升清晰度」5 个一键预设，输出可选「跟随原图」的比例与清晰度。
- 生成页也能挂 1~4 张参考图：挂了就是按参考图生成，不挂就是纯文生图，同一个按钮，不需要切模式。
- 修图的选图区加了「最近上传」快捷条，改刚传上去那张图不用再开一次选图窗从头找。
- AI 次数按月发放（默认每月 50 次、每天最多 5 次，由用户组配置），签到额外随机送 1~3 次；额度按月重置，不累加到下个月。
- 生成失败或上游超时会自动把扣掉的次数退回；另有一道对账，扣了费却没递交出去、或超过一小时没结果的任务会被回收退款——这类任务不回收就是白扣用户的次数。今日已用次数不退，它是每日闸门。
- 可以在设置页关联 pic.bi 账号：本站免费次数用完后还能继续生成（扣 pic.bi 积分），并解锁 4K 档。关联只打通额度，不能用来登录本站，也不会合并账号。

### 变更
- 存储配置（BYOS，自带存储桶）改为 API 令牌也能调用，客户端因此能配桶了；但新建、修改、删除、设为默认这类改动必须附一枚发到账号邮箱的验证码——单靠一枚泄露的令牌把默认桶指向别处，会是一条持久的外泄通道。网页登录会话免验证码，读取列表与连通性测试也不要码。
- 用 API 令牌（PicGo 一类客户端）发起的 AI 生成只能用免费额度与免费清晰度档，不会花掉 pic.bi 积分——那种令牌常年有效，而解绑 pic.bi 只认网页登录。
- 首页加了 Cloudflare Web Analytics 统计脚本，与原有的自建统计并存（多一个第三方域名的请求：static.cloudflareinsights.com）。

> **自建者需动手**
> - AI 功能默认关闭，需配 `APIMART_API_KEY`（可选 `APIMART_BASE_URL`）。不配则接口一律回「未开启」，界面把入口整个藏起来，而不是点了才报错；启动日志里会写明有没有开。
> - 关联 pic.bi 需要 5 个新变量：`PICBI_BASE_URL`、`PICBI_CLIENT_ID`、`PICBI_CLIENT_SECRET`、`PICBI_PARTNER_ID`、`PICBI_PARTNER_SECRET`。OAuth 那对与 partner 那对用途不同，任一组留空对应能力就整个关掉（缺 partner 密钥时 4K 档也一并关闭）；回调地址填 `<PUBLIC_BASE_URL>/auth/picbi/callback`，须与 pic.bi 侧登记的全串一致。
> - 新增 `ai_generations` 表与用户表上的 AI 次数、pic.bi 关联字段，随 AutoMigrate 自动建。

---

## 2026-08-16

### 新增（macOS 客户端）
- 目录监控自动上传：指定文件夹后新增文件自动上传，带本地清单避免重复占用配额；配额耗尽、达每日上限或令牌失效会暂停，每日上限次日自动恢复。刻意做成单向只增：本地删除不动云端，云端删除也不回删本地。
- 图库整库导出到本地文件夹，支持断点续传；中途出错会如实报告「导出中断」而非「完成」。
- 上传前编辑器：裁剪（含比例预设）、马赛克涂抹（像素化或纯色覆盖，可撤销）、旋转、文字水印。水印在设置页配置，监控目录可自动套用（原图模式与动图不加）。马赛克渲染失败时绝不回落上传原件，而是保持编辑器打开并报错。

### 修复
- macOS 客户端多个入口（拖放、编辑器、目录监控）同时触发上传时可能崩溃。

### 移除
- macOS 客户端从本仓库移出，迁到独立仓库 `openimg-app`，40 个提交的历史完整保留，推 `v*` 标签自动出 Release。

> **自建者需动手**
> - 本仓库不再含 `apple/` 目录。服务端为客户端准备的接口（`native_auth`、`apple_assoc`、machine 组）未改动，无需配合调整。

---

## 2026-08-13

### 变更
- **影响面较大**：「附加格式」改名为「上传自动转换」并改变语义——优化模式下选中 WebP/AVIF 后，主图直接编码成该格式存储，不再保留原格式主图，外链扩展名就是目标格式。动图不转换；原图模式下转换设置整体不生效，设置页对应区块置灰。旧语义（主图保原格式、WebP/AVIF 只作附加变体）与「选了 WebP 上传就该是 WebP」的普遍预期相反。
- AVIF 改为上传时同步编码，不再后台异步补变体。选 AVIF 会明显拉长单次上传耗时，设置页文案已提示。
- **接口**：上传接口返回的 `url` 扩展名即目标格式，`variants` / `variant_urls` 里不再出现 webp/avif 这一项（只剩缩略图 `w600`），`size_stored` 相应变小。按旧字段解析响应的脚本需要检查。

### 修复
- 改了转换格式、限宽或上传模式之后重传同一张图，秒传会克隆按旧设置生成的产物，表现为「改了设置没生效」；现在只有处理设置完全一致时才复用，否则重新处理。顺带堵掉了跨模式克隆拿到语义不符字节的老问题（原图模式的用户可能拿到被重编码的字节，优化模式的用户可能拿到 EXIF 俱全的原始字节）。
- 原图模式的说明此前写作「字节不变，保留全部 EXIF 与拍摄地点」，与实际行为不符：JPEG 的 GPS 定位会被抹除，PNG/WebP 等其他格式才是原样公开。文案已改准确——这条直接关系到分享手机照片前的判断。

> **自建者需动手**
> - `images` 表新增 `proc_sig` 列（启动时自动迁移）。存量行该列为空、永不匹配，同一张图第一次重传会按当前设置重新处理一次后收敛。
> - 对象的引用计数与清理判据从 `sha256` 改为 `object_key`：同样的字节在不同设置下会各存一份对象，存储占用可能略增，但清理不会再留下删不掉的孤儿对象。
> - 开发文档重写（技术栈总览、开发约定、完整部署与回滚步骤），并修正了 README 与架构图里几处与生产不符的旧描述。部署前建议按新版走一遍。

---

## 2026-08-12

### 变更
- 未登录导航把「登录」「注册」两个文字链接合并成一个带图标的登录按钮，注册入口保留在登录页底部。

### 修复
- 直接访问 CDN 域名的根路径会返回整个存储桶的对象清单（任何人都能枚举全部对象名），现在改为 301 跳回主站，对象路径不受影响。

> **自建者需动手**
> - 上一条对应 `deploy/caddy/openimg.caddy` 的改动，自建部署需同步；`deploy/deploy.sh` 的验证步骤也随之把 CDN 域名的预期状态码从 200 改成 301。

---

## 2026-08-10

### 新增
- 未登录状态下导航也有语言切换了，此前只有登录后才有。

### 变更
- 导航的语言切换换成圆形国旗按钮，显示的是「点下去会切到哪个语言」。

---

## 2026-08-09

### 新增
- 空间页的签到卡多了一行「累计签到已获得 X」，把每日签到、连续 7 天与月度奖励合并统计；没签到过的人不会看到这行。
- 短链页和图片详情页点图片可以放大了（LiteZoom 灯箱）：滚轮/双指缩放、拖拽平移、键盘与 ESC 关闭，关掉后焦点回到点击前的位置。

### 变更
- 验证码邮件跟随当前主题出绿色或紫色两套配色，正文随界面语言出中文或英文。此前正文写死中文，配色是紫框配绿数字的历史遗留。
- 首页文案整体改写（标题改为「稳定快速的免费图床」，副标题一句话列出压缩转换 / CDN / 外链持久 / API / 自有存储），页面 title 与 meta 描述同步换成更直白的说法。

### 修复
- 验证码邮件里的验证码数字在白底上对比度过低（旧配色只有 1.7:1），换成加深后的可读档。
- 灯箱里右键点击会留下「幽灵指针」，之后单指拖动被误判成双指捏合，图片乱跳。
- 灯箱关闭后仍在加载的大图不再写回已隐藏的视图；Tab 键不会再跑到灯箱背后的页面上；加载失败的图片状态修正。

> **自建者需动手**
> - 前端每个 API 请求会带 `X-Openimg-Brand` 头，服务端据此决定邮件配色（CORS 白名单已加）。若前置网关过滤了这个头，邮件会回落绿色主题，不会报错。

---

## 2026-08-05 ~ 2026-08-07 — 项目起步

站点在 8 月 5 日上线：Go + libvips 后端、React 前端的完整图床，上传自动按 SHA-256 去重、剥 EXIF、按需重编码为 WebP/AVIF 并生成缩略图，支持绑定自有 R2/S3（BYOS，凭据加密存储），改密码、删除账号一类敏感操作走邮箱验证码。每张图有一个 4–6 位短链，打开是一张分享页而不是裸图，页上有 BBCode / HTML / Markdown / 直链等 9 种格式一键复制、5 种表情反应和举报入口，OG 标签在服务端注入，论坛和聊天软件抓得到预览卡片；举报可带分类、允许匿名，新举报邮件通知全部管理员，后台可屏蔽或删除。同期还把 macOS 客户端从菜单栏小工具做成了完整原生客户端（该代码已于 8 月 16 日拆分到独立仓库 `openimg-app`）。

随后两天做的是定版与补齐。视觉上确定单一深色主题、品牌绿 `#5DE31D`（品牌实底一律配近黑文字，白色不出现在任何品牌色表面上），删掉浅色主题，全套 logo、favicon、PWA 与 app 图标改由一条脚本从矢量源重出；上线了不在登录墙后面的 `/docs` 接入文档页（拿 token、一条能跑通的 curl、PicGo 与 Typora 的填法、错误对照表）；做了全站中英双语，前端走词典、后端按 `Accept-Language` 应答，不带语言头时默认中文，所以既有的 PicGo 配置与脚本看到的东西不变。缩略图宽度从 200px 提到 600px 并改为每用户可配（默认 600 + WebP），用户组加了徽章，管理后台的页签变成真实 URL、流水加了分页与搜索，全站弹窗从原生 `alert/confirm` 换成站内样式且自动聚焦永不落在危险按钮上。

这段时间修掉的问题里，几条值得单独记：`/assets/*.js` 被 SPA 兜底吞掉导致整站白屏；PNG 重编码后反而胀大 30–80%（配额按存储占用扣，等于用户为这个失误买单）；限流一直按 Cloudflare 边缘节点的 IP 计数，同一节点后面的所有用户共享每分钟 60 次，后台看到的注册与上传 IP 也全是 CF 的地址；图库与批量删除的搜索没有转义 LIKE 通配符，搜一个 `%` 会命中整个图库；举报对话框拿显示文本当标识符比较，英文界面下选「其他」会被当成选了具体分类。

> **自建者需动手（这一段的历史遗留项）**
> - 拿到真实客户端 IP 需要一对配置同时生效：Caddy 侧剥掉非 Cloudflare 网段来源的 `CF-Connecting-IP`，Go 侧用它作为客户端地址。只改一处，要么限流仍按边缘节点计数，要么任何直连源站的人都能伪造地址。生产用的 Caddy 配置在仓库里（`deploy/caddy/openimg.caddy`）。
> - Passkey 的 RP ID 不再硬编码 `openimg.io`，改为从 `PUBLIC_BASE_URL` 推导。此前部署在别的域名下，Passkey 按钮永远不可能成功，且失败发生在浏览器端，服务端日志里什么都看不到。
> - 8 月 5 日之前的存量数据需要跑一次 `backend/cmd/reprocess`，才能修掉历史 PNG 的膨胀并补上 600px 缩略图（默认演练，`--apply` 才写）。
> - 用户组种子里的 `max_total_space` 全部置 0（无上限），允许格式也有变动。已有的库需要自行调整，只改库不改种子会在下次重建时回退。
> - 缩略图变体名带格式后缀（`w400.avif`），旧的裸宽度名（`w600`，隐含 WebP）继续有效；purge 与备份任务靠这条命名推导对象键，自定义时不要打断它。
> - `deploy/deploy.sh` 会编译到临时路径、失败自动回滚、清 Cloudflare 缓存，并断言返回的 Content-Type 而不只看状态码。清缓存的凭据从不进版本库的 `deploy/.env.local` 读取；取不到 zone id 或 purge 失败会显式告警，但不会让整个部署失败。
> - 存储配置新增 `ThumbBaseURL`，缩略图可以走独立于主图的域名。
> - `/auth/me`、`/api/quota`、`PATCH /api/preferences`、昵称与头像、改密码、Passkey 与 OAuth 管理等接口从 cookie-only 移到 API Token 可达；签发 token、删账号、读写存储密钥仍然只认 cookie。
